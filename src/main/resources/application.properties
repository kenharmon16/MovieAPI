spring.config.import = env.properties
spring.datasource.url=jdbc:mysql://${DB_PORT}${DB_SCHEMA}
spring.datasource.username=${DB_USER_NAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
